class GetArguments():
    def __init__(args):
        self.args = args
        self.arg_dict = {}

    def add_default(arg_name, value):
        self.arg_dict.update([arg_name,value])

    def get_value(arg_name):
        pass

    def update_values():
        self.args.pop(0)
        if len(self.args) // 2 != 0:
            raise Exeption
